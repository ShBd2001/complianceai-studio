[pytest]
testpaths = tests
# Rend le paquet `app` importable quelle que soit la commande utilisee.
# Sans cette ligne, `python -m pytest` fonctionne (il ajoute le dossier courant
# a sys.path) mais `pytest` seul echoue avec ModuleNotFoundError.
pythonpath = .
addopts = -q --tb=short
